<div class="container">
  <div class="badge badge-warning">
    <h3>商品一覧</h3>
  </div>
  <div class="row">
    <div class="col-md-8 offset-md-2">
      <%= render 'index', items: @items %>
      <%= paginate @items %>
    </div>
    <div class="col-md-2">
      <%= link_to new_admin_item_path do %>
        <button type="button" class="btn btn-success btn-circle btn-lg" style="font-weight: bold; border-radius: 50%;">＋</button>
      <% end %>
    </div>
  </div>
</div>
