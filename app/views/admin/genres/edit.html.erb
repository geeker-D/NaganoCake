<div class="container">
  <div class="badge badge-warning">
  <h3>ジャンル一覧・追加</h3>
  </div>
  <div class="field">
    <%= form_with model:@genre do |f| %>
      <%= f.label :name ,"ジャンル名" %>
      <%= f.text_field :name %>
      <%= f.submit "変更を保存" %>
    <% end %>
  </div>
</div>